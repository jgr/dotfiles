#!/bin/sh

git config --global --replace-all user.name "James Rucker"
git config --global --replace-all user.email "james@rucker.land"

echo '~/.gitconfig author written: "James Rucker <james@rucker.land>"'
